build/
*.test
*.out
.idea/
.vscode/
.obsidian/
*.log
.DS_Store

# 부하 테스트 결과 (scripts/load-test.sh)
/logs/

# repo root 에 잘못 떨어진 산출물 — 정식 위치는 build/bin/.
# (cmd/<svc>/ 안에서 또는 root 에서 `go build ./cmd/<svc>` 시 root 에 생성됨)
/mci-api
/mci-push
/mci-admin
/mci-price
/mci-chart
/mci-test
/mci-edge-api
/mci-edge-push
/mci-edge-price
/mci-edge-chart
/mci-edge-tcp
/mci-mds-shim
/quote-forwarder
/dev-bar-faker
/load-gen
/quote-diff
/fx-sync
/mci-edge-fix-md
/mci-edge-fix-ord
/algo-tester
/mock-lp
/fix-tester
/fix-md-tester
/quote-replay
/spot-load-gen
/ws-load-gen
/tcp-tester

# svc-specs 배포용 zip 패키지 — docs/svc-specs/*.md 에서 재생성 (산출물)
docs/svc-specs.zip

# cside C SDK 빌드 산출물 — 소스만 commit, 산출물은 `make cside`/`make wtgprice` 가 재생성
cside/*/*.a
cside/*/*.o
cside/*/sample
cside/*/sample_spot
cside/*/sample_s02
cside/*/sample_s03
cside/*/*.dSYM/

# proto / pb.go 의 임시 백업
*.pb.go.bak
*.proto.bak

# 한글 발표자료 / 임시 자산 — 정식 위치로 옮길 때까지 commit 안 함
/WTG_*.html
/WTG_*.docx
/WTG_*.pptx
/watch.sh

# 로컬 작업 임시 파일 (커밋 금지)
outputs_tmp/
*.pptx
시세배포엔진_설계노트.html
AGENTS.md
